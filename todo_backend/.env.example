# Backend environment variables
# Provide the absolute path to the SQLite DB file created/managed by the database container.
# If not set, the backend will attempt to parse ../database/db_connection.txt or fall back to ../database/myapp.db
SQLITE_DB=/absolute/path/to/myapp.db
